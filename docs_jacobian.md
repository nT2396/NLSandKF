## Jacobian 的核心作用：把"多普勒差多少"翻译成"位置差多少"

没有 Jacobian，你手上有残差 $\Delta\mathbf{f}$（多普勒的误差），但不知道这个误差对应接收机该往哪挪、挪多远。Jacobian 就是这个翻译器。

## 一、整体数据流



```
第132行:  f_model = 理论多普勒(猜测位置)       NK×1
第135行:  df      = y_meas - f_model           NK×1  ← "多普勒差多少"
第152行:  H       = Jacobian                    NK×3  ← "翻译器"
第155行:  dp      = H \ df                      3×1   ← "位置该挪多少"
第160行:  p_u     = p_u + dp'                         ← 更新猜测
```

**要理解的关键方程**（这就是 Taylor 展开的数学表达式）：
$$
$$\underbrace{\Delta\mathbf{f}}*{NK \times 1} = \underbrace{\mathbf{H}}*{NK \times 3} \cdot \underbrace{\Delta\mathbf{p}}_{3 \times 1}$$
$$


## 二、H 的每一行是什么

取一个观测（一行）来看。假设 Sat1 在 t=0 时刻：



```
卫星位置: p_s = [3594.5, -5627.8, -1323.2] km    (ECEF)
卫星速度: v_s = [2.900, 3.242, -5.944] km/s
猜测位置: p_u = 真实位置 + 50km 偏移
```

程序算出这一行 Jacobian 的三个数值（第152行输出的一行）：



```
H 的第 1 行 = [0.3847,  -0.2156,  0.8912]    (Hz/m)
                ∂f_D/∂x   ∂f_D/∂y   ∂f_D/∂z
```

**物理含义**：

| H 数值                   | 含义                                             |
| ------------------------ | ------------------------------------------------ |
| `∂f_D/∂x = 0.3847 Hz/m`  | 接收机沿 ECEF X 轴 +1m，多普勒增加 0.3847 Hz     |
| `∂f_D/∂y = -0.2156 Hz/m` | 接收机沿 ECEF Y 轴 +1m，多普勒**减少** 0.2156 Hz |
| `∂f_D/∂z = 0.8912 Hz/m`  | 接收机沿 ECEF Z 轴 +1m，多普勒增加 0.8912 Hz     |

**这一行告诉你最陡的修正方向**：Z 方向灵敏度最高（0.89 Hz/m），Y 方向最弱（0.22 Hz/m）。

## 三、150-152 行逐行拆解

我以**一行**为例，用具体数字走一遍。

### 输入数据（取 NK=75 行中的第 1 行）



```
v_s = [2900.51, 3242.23, -5943.52]       (1×3, m/s)
l   = P_sat(1,:) - p_u = [lx, ly, lz]     (1×3, m)
d   = vecnorm(l) = 距离                    (标量, m)
```

### 第 150 行：`v_dot_l = sum(V_sat .* l_vec, 2)`



```
V_sat(1,:) = [2900.51,  3242.23,  -5943.52]   ← 卫星速度
l_vec(1,:) = [lx,       ly,       lz      ]   ← 视线向量

逐元素乘: [2900.51*lx, 3242.23*ly, -5943.52*lz]
sum 求和: 2900.51*lx + 3242.23*ly + (-5943.52)*lz

v_dot_l(1) = v_s · l = 径向速度 × 距离
```

**几何意义**：如果接收机到卫星的距离在缩短，
$$
v_r < 0，v_s \cdot l < 0
$$
。

### 第 151 行：`l_dot_term = (v_dot_l ./ (dist.^2)) .* l_vec`



```
v_dot_l / d² = (v_s·l) / d²           ← 标量
然后 × l_vec 的每一列:

l_dot_term(1,:) = [(v_s·l)/d² * lx,   (v_s·l)/d² * ly,   (v_s·l)/d² * lz]
                       ↑                   ↑                   ↑
                   X 方向分量           Y 方向分量           Z 方向分量
```

**这个项来自距离对位置的偏导**。它修正的是"接收机挪动改变了到卫星的距离 → 距离改变影响了多普勒"这层间接效应。

### 第 152 行：`H = (f_c ./ (c .* dist)) .* (V_sat - l_dot_term)`



```
                  缩放因子             方向差

H(1,:) =  fc/(c·d)  ·  [ v_sx - (v_s·l)/d²*lx,    ← X 分量
                           v_sy - (v_s·l)/d²*ly,    ← Y 分量  
                           v_sz - (v_s·l)/d²*lz ]   ← Z 分量
```

对应之前推导的公式：
$$
\frac{\partial f_D}{\partial \mathbf{p}_u} = \frac{f_c}{c \cdot d} \left[ \mathbf{v}_s^T - \frac{\mathbf{v}_s \cdot \mathbf{l}}{d^2} \cdot \mathbf{l}^T \right]
$$
$$

## 四、第 155 行 `dp = H \ df` 到底在解什么

NK=75 行，3 个未知数（超定），$H \backslash df$ 做最小二乘：
$$
\Delta\mathbf{p} = (\mathbf{H}^T\mathbf{H})^{-1}\mathbf{H}^T \Delta\mathbf{f}
$$
$$

这是**75 个传感器给 75 个读数，投票决定位置往哪挪**：



```
每个观测(每行)都说: "根据我的灵敏度方向，你应该往我的方向挪 X 米"
                    ↓
所有观测加权投票 ──→ 最小二乘给出最优的 3D 修正向量
```

**由于每颗卫星在每个时刻的视线方向不同**，它们的灵敏方向也不同：



```
Sat1 @ t=0:  灵敏度方向 ≈ [ 0.38, -0.22,  0.89]  ← 主要对 Z 敏感
Sat1 @ t=10: 灵敏度方向 ≈ [ 0.31, -0.18,  0.93]  ← 方向略有变化
Sat2 @ t=0:  灵敏度方向 ≈ [-0.52,  0.41, -0.75]  ← 完全不同的方向!
Sat2 @ t=10: 灵敏度方向 ≈ [-0.47,  0.38, -0.80]
...
75 行, 75 个不同的灵敏方向
```

**不同方向的灵敏度向量越多，H 矩阵的条件数越好，求解越稳定**。这就是为什么需要多时刻—不同时刻同一颗卫星的视线方向不同，灵敏方向也在转。

## 五、H 的完整结构



```
H (NK×3) =
┌                                        ┐
│ ∂f_D1/∂x   ∂f_D1/∂y   ∂f_D1/∂z      │  ← Sat1 @ t0
│ ∂f_D2/∂x   ∂f_D2/∂y   ∂f_D2/∂z      │  ← Sat1 @ t1
│    ...        ...        ...          │
│ ∂f_Dn/∂x   ∂f_Dn/∂y   ∂f_Dn/∂z      │  ← Sat3 @ t24
└                                        ┘
     ↑            ↑           ↑
   X方向敏      Y方向敏     Z方向敏
   感度向量    感度向量    感度向量
```

| 数值的量级        | 含义                                    |
| ----------------- | --------------------------------------- |
| 约 0.1-1 Hz/m     | 单行：接收机挪 1m，该观测的多普勒变化   |
| 约 1e5 Hz (残差)  | 第 1 次迭代：多普勒误差的总量           |
| 约 1.5e5 m (修正) | 第 1 次迭代：位置修正量 ≈ 残差 / 灵敏度 |

## 六、迭代过程中的动态变化



```
迭代 1:
  p_u 离真实位置 148km → l_vec 全是错的 → 算出的 H 也是基于错误位置
  → dp ≈ 151km (大步修正, 因为残差大)
  
迭代 2:
  p_u 离真实位置 31km → l_vec 更准确了 → H 更准确
  → dp ≈ 31km
  
迭代 3:
  p_u 离真实位置 0.4km → H 几乎准确
  → dp ≈ 421m (残差急剧缩小)
  
迭代 5:
  p_u 离真实位置 0.0001km → H 完全准确 (局部线性度极高)
  → dp ≈ 3e-9 m (收敛)
```

**每次迭代都重新算 H**，因为 H 依赖于当前猜测位置（通过 l_vec）。猜测位置变了，l_vec 变了，H 也变了——这就是 Gauss-Newton 不同于普通 Newton 法的关键。

```
★ Insight ─────────────────────────────────────
```

- **Jacobian 是 Gauss-Newton 的唯一"引擎"**：没有它，你只有残差，不知道方向；有了它，每一步都知道"往哪走"。所有非线性优化方法（Gauss-Newton、Levenberg-Marquardt、Trust-Region）的区别只在于如何用 Jacobian 算修正量，但 Jacobian 本身都一样的。
- **`H \ df` 做了两件事**：(1) 加权投票——灵敏度高的方向权重大，灵敏度低的方向权重小；(2) 去耦合——X/Y/Z 的修正量不是独立算的，而是联合求解，因为各方向之间有相关（改变 X 同时影响多个观测的多普勒）。
- **物理直觉**：假设你蒙着眼睛站在一个未知位置，75 个人（75 个观测）同时告诉你"往左 3 步"、"往右 1 步"、"往前 2 步"……每个人说的方向和步数都不同。你怎么办？你根据每个人和你之间的角度（Jacobian 的每一行），加权平均所有人的建议，走最优的 1 步。重复这个过程，最终站到正确位置。这就是 Gauss-Newton。`─────────────────────────────────────────────────`